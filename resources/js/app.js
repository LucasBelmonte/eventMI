require('./bootstrap');

$('.carousel').carousel({
    interval: 2000,
  })


  const query = window.matchMedia(
      '(prefers-color-scheme: dark)'
  );

  query.matches

  query.addEventListener('change', res =>{

  })
